on: 
  push:
    branches:
      - main
name: merge master
jobs:
  firstJob:
      name: Echo GITHUB_HEAD_REF
      runs-on: ubuntu-latest
      steps:
      - run: CONTENTS=$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH") | echo $CONTENTS
